import React from 'react';
import styles from './styles.module.css';

export default () => (
  <div className={styles.container}>
    <div className={styles.state}>Game status</div>
    <div className={styles.dashboard}>Dashboard</div>
  </div>
);
